# Tests (V1)

These are lightweight pytest checks used for:
- schema sanity
- label distribution sanity
- processed feature table sanity

Run from repo root:
```bash
pytest -q v1_mca_baseline/tests
```
