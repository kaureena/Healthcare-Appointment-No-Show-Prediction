# Experiments (V1)

This folder mimics lightweight experiment tracking suitable for an academic project.

- `experiment_registry.csv` is a simple index of baseline runs
- `runs/` contains per-run parameter and metric JSON files

V2 uses a more formal registry and monitoring signals.
